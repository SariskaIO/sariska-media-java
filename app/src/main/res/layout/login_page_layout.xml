<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:contentDescription="Logo"
        android:scaleType="centerCrop"
        android:src="@drawable/sariskalogo"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal = "true"/>

    <TextView
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_below="@id/logo"
        android:id="@+id/signin"
        android:text="Create Meeting"
        android:textColor="@color/white"
        android:textSize="35dp"
        android:layout_margin="50dp"
        android:gravity="center"
        android:textStyle="bold"
        android:fontFamily="sans-serif"/>

    <LinearLayout
        android:id="@+id/options"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/logo"
        android:layout_marginTop="95dp"
        android:layout_centerHorizontal="true">

        <ImageView
            android:id="@+id/landingMute"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_margin="20dp"
            android:contentDescription="Landing Mute"
            android:src="@drawable/ic_baseline_mic_24" />

        <ImageView
            android:id="@+id/landingVideoMute"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_margin="20dp"
            android:contentDescription="Landing Mute Video"
            android:src="@drawable/ic_baseline_videocam_24" />

        <ImageView
            android:id="@+id/speakerOnOff"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_margin="20dp"
            android:contentDescription="Landing Switch Camera"
            android:src="@drawable/ic_baseline_volume_mute_24" />
    </LinearLayout>

    <EditText
        android:id="@+id/roomName"
        android:layout_width="350dp"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_below="@id/options"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:background="#30ffffff"
        android:drawableLeft="@drawable/ic_baseline_meeting_room_24"
        android:drawablePadding="20dp"
        android:fontFamily="sans-serif"
        android:hint="Room Name"
        android:padding="20dp"
        android:singleLine="true"
        android:textColor="@color/white"
        android:textColorHint="@color/white" />

    <EditText
        android:layout_height="wrap_content"
        android:layout_width="350dp"
        android:layout_centerHorizontal="true"
        android:id="@+id/username"
        android:layout_below="@id/roomName"
        android:background="#30ffffff"
        android:hint="Username"
        android:textColorHint="@color/white"
        android:textColor="@color/white"
        android:layout_margin="10dp"
        android:padding="20dp"
        android:drawableLeft="@drawable/ic_baseline_person_outline_24"
        android:drawablePadding="20dp"
        android:fontFamily="sans-serif"
        android:singleLine="true"/>

    <com.google.android.material.button.MaterialButton
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/loginButton"
        android:text="Start Meeting"
        android:textColor="@color/black"
        android:layout_below="@id/username"
        android:backgroundTint="#90ffffff"
        android:layout_centerHorizontal="true"
        android:layout_margin="20dp"
        android:fontFamily="sans-serif"
        android:textStyle="bold"/>

</RelativeLayout>